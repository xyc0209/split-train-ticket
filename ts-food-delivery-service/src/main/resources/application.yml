server:
  port: 8096

spring:
  cloud:
    nacos:
      discovery:
          server-addr: ${NACOS_ADDRS:1.94.55.43:8848}
#          server-addr: 10.176.122.80:8848
  application:
    name: ts-food-delivery-service
  datasource:
#    url: jdbc:mysql://localhost:30001/ts-food-delivery-mysql?useSSL=false
    url: jdbc:mysql://${FOOD_DELIVERY_MYSQL_HOST:1.94.55.43}:${FOOD_DELIVERY_MYSQL_PORT:31306}/${FOOD_DELIVERY_MYSQL_DATABASE:ts_food_mysql}?useSSL=false
    username: ${FOOD_DELIVERY_MYSQL_USER:root}
    password: ${FOOD_DELIVERY_MYSQL_PASSWORD:mysql@12345}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

swagger:
  controllerPackage: fooddelivery.controller