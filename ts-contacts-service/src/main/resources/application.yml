server:
  port: 8095

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:1.94.55.43:8848}
  application:
    name: ts-contacts-service
  datasource:
    url: jdbc:mysql://${CONTACTS_MYSQL_HOST:1.94.55.43}:${CONTACTS_MYSQL_PORT:31306}/${CONTACTS_MYSQL_DATABASE:ts_contacts_mysql}?useSSL=false
    username: ${CONTACTS_MYSQL_USER:root}
    password: ${CONTACTS_MYSQL_PASSWORD:mysql@12345}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect


swagger:
  controllerPackage: contacts.controller