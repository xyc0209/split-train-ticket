spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:1.94.55.43:8848}
  application:
    name: ts-price-service
  datasource:
    url: jdbc:mysql://${PRICE_MYSQL_HOST:1.94.55.43}:${PRICE_MYSQL_PORT:31306}/${PRICE_MYSQL_DATABASE:ts}?useSSL=false
    username: ${PRICE_MYSQL_USER:root}
    password: ${PRICE_MYSQL_PASSWORD:mysql@12345}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect


# HTTP Server
server:
  port: 8099  # HTTP (Tomcat) port

swagger:
  controllerPackage: price.controller